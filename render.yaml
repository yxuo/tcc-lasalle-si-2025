services:
  # Neon e Vercel sao configurados externamente nos respectivos paineis.
  - type: web
    name: rotinar-api
    runtime: node
    buildCommand: npm install && npm run prisma:generate --workspace=@rotinar/api && npm run prisma:migrate:deploy --workspace=@rotinar/api && npm run build --workspace=@rotinar/api
    startCommand: npm run start --workspace=@rotinar/api
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: WEB_ORIGIN
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
